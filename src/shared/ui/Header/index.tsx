import styles from '@/shared/ui/Header/index.module.scss';

const Header = () => {
    return (
        <div className={styles.headerContainer}>
            <h1>ToDo Lists</h1>
        </div>
    )
};

export { Header };
